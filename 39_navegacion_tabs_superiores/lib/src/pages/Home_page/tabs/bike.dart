import 'package:flutter/material.dart';

class BikeW extends StatelessWidget {
  const BikeW({super.key});

  @override
  Widget build(BuildContext context) {
      return const Center(
        child: Text('Mi bici...'),
        );
  }
}