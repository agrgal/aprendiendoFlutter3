import 'package:flutter/material.dart';

class CarW extends StatelessWidget {
  const CarW({super.key});

  @override
  Widget build(BuildContext context) {
      return const Center(
        child: Text('Coche'),
        );
  }
}