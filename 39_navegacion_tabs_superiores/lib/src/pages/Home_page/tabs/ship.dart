import 'package:flutter/material.dart';

class ShipW extends StatelessWidget {
  const ShipW({super.key});

  @override
  Widget build(BuildContext context) {
      return const Center(
        child: Text('Mi barco...'),
        );
  }
}